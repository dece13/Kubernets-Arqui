---
# Secret for database credentials (base64 encoded)
# ⚠️ IMPORTANT: This is a TEMPLATE with example values for demo purposes only
# ⚠️ DO NOT use these default credentials in production!
# Replace with secure credentials before deploying to production
# To encode: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: demo-app-db-secret
  namespace: demo-app
  labels:
    app: demo-app
type: Opaque
data:
  # Default values for demo/testing only (postgres/postgres)
  # ⚠️ REPLACE THESE VALUES IN PRODUCTION
  # To generate new values:
  #   echo -n 'your-secure-username' | base64
  #   echo -n 'your-secure-password' | base64
  DB_USER: cG9zdGdyZXM=
  DB_PASSWORD: cG9zdGdyZXM=
